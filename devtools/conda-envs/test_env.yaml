name: openff-units-test
channels:
  - conda-forge
  - conda-forge/openff-units_dev
dependencies:
  - python
  - pip
  - versioningit

  - numpy
  - pint
  - openff-utilities >=0.1.3

    # Tests
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
  - uncertainties

    # Typing
  - mypy
  - types-setuptools
