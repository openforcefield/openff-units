name: openff-units-test
channels:
  - conda-forge
dependencies:
  - python <3.13
  - pip
  - versioningit

  - numpy
  - pint
  - openff-utilities >=0.1.3

    # Tests
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
  - uncertainties

    # Typing
  - mypy
  - types-setuptools
