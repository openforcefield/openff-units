name: openff-units-test
channels:
  - conda-forge
dependencies:
  - python
  - pip

  - numpy
  - pint =0.25
  - openff-utilities >=0.1.3

    # Tests
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
  - uncertainties

    # Typing
  - mypy
  - types-setuptools
