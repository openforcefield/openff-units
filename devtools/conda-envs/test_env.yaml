name: openff-units-test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - pint >=0.20.0
  - openff-utilities >=0.1.3

    # Tests
  - pytest
  - pytest-cov
  - codecov
  - uncertainties

    # Typing
  - mypy
