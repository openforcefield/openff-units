name: openff-units-test
channels:
  - conda-forge
dependencies:
  - python
  - pip
  - versioningit

  - numpy
  - pint
  - openff-utilities >=0.1.3

    # Tests
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
  - uncertainties

    # Typing
  - mypy ~=1.13
  - types-setuptools
