name: openff-units-test
channels:
  - conda-forge
  # conda-forge/label/openff-units_dev
dependencies:
  - python
  - pip

  - numpy
  - pint
  - openff-utilities >=0.1.3

    # Tests
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
  - uncertainties

    # Typing
  - mypy
  - types-setuptools
