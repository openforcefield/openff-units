name: openff-units-test
channels:
  - conda-forge
dependencies:
  - python
  - pip
  - numpy
  - pint
  - openff-utilities >=0.1.3

    # Tests
  - pytest
  - pytest-cov
  - pytest-nauto
  - pytest-randomly
  - uncertainties

    # Typing
  - mypy
