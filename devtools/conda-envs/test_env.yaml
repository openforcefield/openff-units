name: openff-units-test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - pint >=0.16.1

    # Tests
  - pytest
  - pytest-cov
  - codecov
  - uncertainties
  - openmm

    # Typing
  - mypy
  - typing-extensions
  - types-setuptools
  - types-pkg_resources
