name: openff-units-test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - pint >=0.16.1

    # Tests
  - pytest
  - pytest-cov
  - codecov
  - mypy
  - uncertainties
  - openmm
